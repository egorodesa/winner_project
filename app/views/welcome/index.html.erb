List of all registered users
<p>

<%= paginate @users %>

<% @users.each do |user| %>
  <strong>user id :</strong> <%= user.id %>,
  <strong> user email: </strong><%= user.email %>
  <%= link_to "<-> chat with", chatting_url(:receiver_id => user.id) unless user == current_user%> <br />
<% end %>

